/**
 * Server-side auth instance for the platform app.
 * Use in server components, server actions, route handlers.
 */

export { platformAuth as auth } from '@rp/auth-core/platform';
export type { PlatformSession as Session } from '@rp/auth-core/platform';
